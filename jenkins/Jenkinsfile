pipeline {
    agent {
	dockerfile {
	    dir 'jenkins'
	}
    }

    stages {
	stage('3rd Party Libs') {
	    steps {
		script {
		    sh """
			mkdir build
			cd build
			conan install .. --build missing
			"""
		}
	    }
	}
    }
}
